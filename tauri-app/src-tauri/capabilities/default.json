{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the MRI Pipeline desktop shell.",
  "windows": ["main"],
  "permissions": ["opener:default"]
}
